---
type: scorecard
title: Average Household Size
description: Average number of persons per household across all listing areas
---

Displays the overall average household size (persons per household).

Calculated as `SUM(total_household_members) / COUNT(households)` across all listed households. Renders as a big-number scorecard showing the average with optional trend comparison against previous data.
